[package]
name = "tf-revoke-redis"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "TrustForge Redis-backed revocation cache (RevocationCache trait only)."

[dependencies]
redis = { version = "0.27", default-features = false }
thiserror = "1"
tf-types = { path = "../tf-types" }
