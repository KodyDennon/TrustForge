{
  "name": "tf-evidence",
  "version": "0.1.0",
  "type": "module",
  "description": "TrustForge compliance evidence CLI: assemble / verify / seal / open / anchor / replay / redact.",
  "main": "./src/cli.ts",
  "scripts": {
    "typecheck": "bun x tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "tf-types": "workspace:*",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.6.3"
  }
}
