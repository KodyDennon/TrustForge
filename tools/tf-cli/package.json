{
  "name": "tf-cli",
  "version": "0.1.0",
  "type": "module",
  "description": "Unified tf command — wraps schema/proof/daemon/session/policy/actor/approval operations.",
  "scripts": {
    "typecheck": "bun x tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "tf-conformance": "workspace:*",
    "tf-session": "workspace:*",
    "tf-types": "workspace:*",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.6.3"
  }
}
