// GENERATED by `tf-schema codegen --target ts` — DO NOT EDIT BY HAND.

import type { ActorId, SignatureEnvelope, Timestamp, TrustDomain } from "./_common.js";

/** Bounded-validity revocation list distributed for offline / constrained deployments (TF-0011 "offline revocation limits"). Verifiers refuse to honour packets whose authority appears in this list, but the list itself expires so a stale list cannot be used forever. */
export interface OfflineRevocationList {
  /** Version of the offline-revocation-list schema. */
  list_version: OfflineRevocationList_ListVersion;
  trust_domain: TrustDomain;
  issued_at: Timestamp;
  valid_until: Timestamp;
  issuer: ActorId;
  /** Entries listed in this offline revocation list. */
  revoked: RevokedEntry[];
  signature: SignatureEnvelope;
}

/** Version of the offline-revocation-list schema. */
export type OfflineRevocationList_ListVersion =
  | "1";

export interface RevokedEntry {
  /** What is being revoked. */
  kind: RevokedEntry_Kind;
  /** Stable identifier of the revoked object. */
  id: string;
  /** Free-text reason. */
  reason?: string;
  revoked_at?: Timestamp;
}

/** What is being revoked. */
export type RevokedEntry_Kind =
  | "actor"
  | "instance"
  | "capability"
  | "delegation"
  | "key";
