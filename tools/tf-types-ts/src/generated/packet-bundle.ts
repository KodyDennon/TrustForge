// GENERATED by `tf-schema codegen --target ts` — DO NOT EDIT BY HAND.

import type { SignatureEnvelope, Timestamp } from "./_common.js";
import type { Packet } from "./packet.js";

/** A group of related packets (e.g. emergency packet + post-event quorum review) shipped together as a unit, for store-and-forward / air-gap workflows. */
export interface PacketBundle {
  /** Version of the packet-bundle schema itself. */
  bundle_version: PacketBundle_BundleVersion;
  /** Stable bundle identifier. */
  bundle_id: string;
  /** Human-readable bundle label. */
  label?: string;
  /** Packets carried by this bundle. */
  packets: Packet[];
  /** How the bundle is being moved. */
  transport_hint?: PacketBundle_TransportHint;
  created_at?: Timestamp;
  signature: SignatureEnvelope;
}

/** Version of the packet-bundle schema itself. */
export type PacketBundle_BundleVersion =
  | "1";

/** How the bundle is being moved. */
export type PacketBundle_TransportHint =
  | "usb"
  | "qr-code"
  | "serial"
  | "lora"
  | "file-drop"
  | "manual";
