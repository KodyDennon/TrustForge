// GENERATED by `tf-schema codegen --target ts` — DO NOT EDIT BY HAND.

import type { ActorId, SignatureEnvelope, Timestamp } from "./_common.js";
import type { ApprovalRequest } from "./approval-request.js";

/** A signed response to an ApprovalRequest. */
export interface ApprovalResponse {
  /** Version of the approval-response schema itself. */
  response_version: ApprovalResponse_ResponseVersion;
  /** Identifier of the ApprovalRequest this responds to. */
  request_id: string;
  /** The human's decision. */
  decision: ApprovalResponse_Decision;
  /** Actor that signed this response. */
  responder: ActorId;
  /** Optional free-text note from the responder. */
  note?: string;
  /** When the response was signed. */
  signed_at: Timestamp;
  /** Signature envelope over the canonical form of this response with signature.signature cleared. */
  signature: SignatureEnvelope;
}

/** The human's decision. */
export type ApprovalResponse_Decision =
  | "approve"
  | "deny";

/** Version of the approval-response schema itself. */
export type ApprovalResponse_ResponseVersion =
  | "1";
