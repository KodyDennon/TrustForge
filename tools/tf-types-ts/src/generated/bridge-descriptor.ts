// GENERATED by `tf-schema codegen --target ts` — DO NOT EDIT BY HAND.

import type { Capability, TrustDomain } from "./_common.js";

/** Declarative descriptor for a TrustForge compatibility bridge. Concrete bridges implement the TS Bridge interface or the Rust Bridge trait and are registered by BridgeRegistry. */
export interface BridgeDescriptor {
  /** Version of the bridge-descriptor schema itself. */
  bridge_version: BridgeDescriptor_BridgeVersion;
  /** Stable kebab-case bridge identifier. */
  bridge_id: string;
  /** Which foreign standard this bridge wires into TrustForge. */
  kind: BridgeDescriptor_Kind;
  /** TrustForge trust domain that accepts input from this bridge. */
  trust_domain: TrustDomain;
  /** Human-readable description of what the bridge accepts and what it emits. */
  description?: string;
  /** Kind-specific configuration. Each bridge validates its own config shape at runtime. */
  config?: Record<string, unknown>;
  /** Optional capabilities this bridge may claim or issue on behalf of the foreign protocol. */
  capabilities?: Capability[];
}

/** Version of the bridge-descriptor schema itself. */
export type BridgeDescriptor_BridgeVersion =
  | "1";

/** Which foreign standard this bridge wires into TrustForge. */
export type BridgeDescriptor_Kind =
  | "spiffe"
  | "webauthn"
  | "mcp"
  | "oauth"
  | "tls";
