{
  "name": "tf-session",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "TrustForge session protocol over WebSocket.",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "bun x tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "tf-types": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.6.3"
  }
}
