[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "trustforge-flask"
version = "0.1.0"
description = "Flask extension and decorator for TrustForge tf-daemon."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
dependencies = [
    "trustforge-client",
    "Flask>=3",
]

[project.optional-dependencies]
test = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
]

[tool.setuptools.packages.find]
include = ["trustforge_flask*"]
exclude = ["tests*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
