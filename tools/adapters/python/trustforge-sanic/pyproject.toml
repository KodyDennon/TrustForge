[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "trustforge-sanic"
version = "0.1.0"
description = "Sanic middleware for TrustForge tf-daemon."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
dependencies = [
    "trustforge-client",
    "sanic>=23",
]

[project.optional-dependencies]
test = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "sanic-testing>=24",
]

[tool.setuptools.packages.find]
include = ["trustforge_sanic*"]
exclude = ["tests*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
