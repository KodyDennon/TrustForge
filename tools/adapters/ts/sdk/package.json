{
  "name": "@trustforge/sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TrustForge core SDK: thin TypeScript client for tf-daemon HTTP endpoints (decide, credentials, proofs).",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "bun x tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@trustforge/test-utils": "workspace:*",
    "@types/bun": "^1.3.9",
    "typescript": "^5.6.3"
  }
}
