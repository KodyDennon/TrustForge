{
  "name": "@trustforge/firebase-auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TrustForge integration for Firebase Auth — verifies Firebase ID tokens via firebase-admin and projects uid into a TrustForge actor.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "bun x tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@trustforge/sdk": "workspace:*"
  },
  "peerDependencies": {
    "firebase-admin": ">=11.0.0"
  },
  "peerDependenciesMeta": {
    "firebase-admin": { "optional": true }
  },
  "devDependencies": {
    "@trustforge/test-utils": "workspace:*",
    "@types/bun": "^1.3.9",
    "typescript": "^5.6.3"
  }
}
