{
  "name": "@trustforge-protocol/hono",
  "version": "0.1.0",
  "type": "module",
  "description": "TrustForge Hono middleware: gates every request through tf-daemon /v1/decide.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "bun x tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@trustforge-protocol/sdk": "workspace:*"
  },
  "peerDependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@trustforge-protocol/test-utils": "workspace:*",
    "@types/bun": "^1.3.9",
    "hono": "^4.6.0",
    "typescript": "^5.6.3"
  }
}
