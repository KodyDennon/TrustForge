{
  "name": "@trustforge-protocol/test-utils",
  "version": "0.1.0",
  "type": "module",
  "description": "Shared test utilities for TrustForge TS adapters (mock daemon, fixtures).",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./mockDaemon": "./src/mockDaemon.ts"
  },
  "scripts": {
    "typecheck": "bun x tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "typescript": "^5.6.3"
  }
}
