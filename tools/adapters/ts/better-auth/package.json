{
  "name": "@trustforge/better-auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TrustForge plugin for Better Auth — projects resolved sessions into TrustForge actors and capabilities.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "bun x tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@trustforge/sdk": "workspace:*"
  },
  "peerDependencies": {
    "better-auth": ">=1.0.0"
  },
  "peerDependenciesMeta": {
    "better-auth": { "optional": true }
  },
  "devDependencies": {
    "@trustforge/test-utils": "workspace:*",
    "@types/bun": "^1.3.9",
    "typescript": "^5.6.3"
  }
}
