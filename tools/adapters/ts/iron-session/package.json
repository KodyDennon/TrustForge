{
  "name": "@trustforge-protocol/iron-session",
  "version": "0.1.0",
  "type": "module",
  "description": "TrustForge integration for iron-session — projects iron-session reads into TrustForge actors.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "bun x tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@trustforge-protocol/sdk": "workspace:*"
  },
  "peerDependencies": {
    "iron-session": ">=8.0.0"
  },
  "peerDependenciesMeta": {
    "iron-session": {
      "optional": true
    }
  },
  "devDependencies": {
    "@trustforge-protocol/test-utils": "workspace:*",
    "@types/bun": "^1.3.9",
    "typescript": "^5.6.3"
  }
}
