{
  "name": "@trustforge/bun-serve",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TrustForge wrapper for Bun.serve({ fetch }) — gates requests through tf-daemon /v1/decide before the user handler runs.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun x tsc -p tsconfig.json --noEmit",
    "typecheck": "bun x tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@trustforge/sdk": "workspace:*"
  },
  "devDependencies": {
    "@trustforge/test-utils": "workspace:*",
    "@types/bun": "^1.3.9",
    "typescript": "^5.6.3"
  }
}
