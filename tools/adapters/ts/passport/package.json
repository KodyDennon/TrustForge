{
  "name": "@trustforge-protocol/passport",
  "version": "0.1.0",
  "type": "module",
  "description": "TrustForge Passport strategy — verifies an opaque session/bearer credential and projects it to a TrustForge actor.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "bun x tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@trustforge-protocol/sdk": "workspace:*"
  },
  "peerDependencies": {
    "passport": ">=0.6.0",
    "passport-strategy": ">=1.0.0"
  },
  "peerDependenciesMeta": {
    "passport": {
      "optional": true
    },
    "passport-strategy": {
      "optional": true
    }
  },
  "devDependencies": {
    "@trustforge-protocol/test-utils": "workspace:*",
    "@types/bun": "^1.3.9",
    "typescript": "^5.6.3"
  }
}
