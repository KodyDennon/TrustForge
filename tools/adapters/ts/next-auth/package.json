{
  "name": "@trustforge/next-auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TrustForge callbacks for NextAuth / Auth.js — projects JWTs and sessions into TrustForge actors.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "bun x tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@trustforge/sdk": "workspace:*"
  },
  "peerDependencies": {
    "next-auth": ">=4.0.0"
  },
  "peerDependenciesMeta": {
    "next-auth": { "optional": true }
  },
  "devDependencies": {
    "@trustforge/test-utils": "workspace:*",
    "@types/bun": "^1.3.9",
    "typescript": "^5.6.3"
  }
}
