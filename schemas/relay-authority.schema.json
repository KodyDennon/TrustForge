{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trustforge.io/schemas/v0/relay-authority.schema.json",
  "title": "TrustForge Relay Authority",
  "description": "Encodes the distinction between forwarding authority and action authority. A relay can carry signed/encrypted TrustForge packets without ever being able to decrypt, authorize, or execute them. The authority record is what the daemon checks before letting a relay forward packets on its behalf, and the proof event a relay emits when it forwards is bound to this record.",
  "type": "object",
  "required": [
    "relay_authority_version",
    "relay",
    "trust_domain",
    "kinds",
    "valid_from",
    "issuer",
    "signature"
  ],
  "additionalProperties": false,
  "properties": {
    "relay_authority_version": {
      "enum": ["1"],
      "description": "Version of the relay-authority schema itself."
    },
    "relay": {
      "$ref": "_common.schema.json#/$defs/ActorId",
      "description": "Relay actor URI (must be of type `relay`)."
    },
    "trust_domain": { "$ref": "_common.schema.json#/$defs/TrustDomain" },
    "kinds": {
      "type": "array",
      "minItems": 1,
      "items": {
        "enum": [
          "forward-only",
          "store-and-forward",
          "fragment-reassemble",
          "priority-queue",
          "emergency-route",
          "lora-relay",
          "matrix-relay",
          "internet-relay"
        ]
      },
      "description": "What kinds of relaying this authority permits. Forwarding authority does NOT include action authority — relays can never decrypt or execute the packets they carry."
    },
    "max_hop_count": {
      "type": "integer",
      "minimum": 1,
      "description": "Maximum number of hops a packet can traverse via this relay."
    },
    "rate_limit_per_minute": {
      "type": "integer",
      "minimum": 1,
      "description": "Caller-observed rate limit; relays exceeding this lose authority."
    },
    "valid_from": { "$ref": "_common.schema.json#/$defs/Timestamp" },
    "valid_until": { "$ref": "_common.schema.json#/$defs/Timestamp" },
    "issuer": {
      "$ref": "_common.schema.json#/$defs/ActorId",
      "description": "Trust-domain authority that issued this relay grant."
    },
    "constraints": {
      "type": "array",
      "items": { "$ref": "_common.schema.json#/$defs/Constraint" },
      "description": "Additional constraints (time window, target glob, ...)."
    },
    "signature": { "$ref": "_common.schema.json#/$defs/SignatureEnvelope" }
  }
}
