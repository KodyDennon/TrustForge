# Schemas

This directory will hold machine-readable schemas for actor identity, actor instance identity, capabilities, negative capabilities, policy decisions, proof events, proof bundles, Agent Contracts, threat models, ProofRPC services, plugin manifests, and conformance tests.
