daemon_version: "1"
self_actor: "tf:actor:service:example.com/tf-daemon"
listen:
  kind: websocket
  bind: "127.0.0.1"
  port: 0
vault:
  path: ".tf/vault.json"
contract_path: ".tf/agent-contract.yaml"
proof_log_path: ".tf/proof.tflog"
approval_queue:
  max_pending: 32
  default_timeout_seconds: 300
