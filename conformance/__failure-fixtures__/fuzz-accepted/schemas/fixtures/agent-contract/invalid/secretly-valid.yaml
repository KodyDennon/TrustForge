# Synthetic failure fixture for tools/tf-conformance/tests/failure-paths.test.ts.
#
# This file lives under `invalid/` but is in fact a perfectly legal
# agent-contract document. A correct fuzz runner must FAIL when AJV
# accepts a fixture that lives under `invalid/` — otherwise a regression
# that makes the schema accept malformed input would slip through
# unnoticed (FIND-005).
contract_version: "1"
spec_version: TF-0006-draft
project: synthetic-fuzz-bait
trust_domain: example.com
actions:
  - name: file.read
    risk: R0
    approval: none
